import { Config } from '@oclif/core';
interface DevOptions {
    commandConfig: Config;
    directory: string;
    force: boolean;
    host: boolean;
    open: boolean;
}
declare function dev({ commandConfig, directory, force, host, open }: DevOptions): Promise<void>;
export default dev;
