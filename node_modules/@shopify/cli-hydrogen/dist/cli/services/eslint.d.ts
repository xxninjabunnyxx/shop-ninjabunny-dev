import { HydrogenApp } from '../models/hydrogen';
interface AddESlintOptions {
    app: HydrogenApp;
    force: boolean;
    install: boolean;
}
export declare function addESLint({ app, force, install }: AddESlintOptions): Promise<void>;
export {};
