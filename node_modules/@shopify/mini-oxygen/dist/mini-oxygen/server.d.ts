/// <reference types="node" />
import http from 'http';
import type { MiniOxygen } from './core';
export interface MiniOxygenServerOptions {
    assetsDir?: string;
    autoReload?: boolean;
    publicPath?: string;
}
export declare function createServer(mf: MiniOxygen, { assetsDir, publicPath, autoReload }: MiniOxygenServerOptions): http.Server;
